import { continents } from './continents'
import { countries } from './countries'
import { languages } from './languages'

export const data = {
  continents,
  countries,
  languages
}

export default data
